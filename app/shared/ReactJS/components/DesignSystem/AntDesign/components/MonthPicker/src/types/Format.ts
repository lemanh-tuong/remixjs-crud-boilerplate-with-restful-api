/**
 * A type representing various month and year format combinations.
 *
 * The possible values are:
 * - 'MM/YYYY'
 * - 'MM.YYYY'
 * - 'MM-YYYY'
 * - 'YYYY/MM'
 * - 'YYYY.MM'
 * - 'YYYY-MM'
 *
 * @typedef {'MM/YYYY' | 'MM.YYYY' | 'MM-YYYY' | 'YYYY/MM' | 'YYYY.MM' | 'YYYY-MM'} Format
 */
export type Format = 'MM/YYYY' | 'MM.YYYY' | 'MM-YYYY' | 'YYYY/MM' | 'YYYY.MM' | 'YYYY-MM';
